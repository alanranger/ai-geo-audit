Rollback (Authority backlinks = DFS)

1. Restore files from this folder:
   - api/aigeo/backlink-metrics.js <= backlink-metrics.js.bak
   - audit-dashboard.html <= audit-dashboard.html.bak

2. Optional without file restore: set Vercel env
   AUTHORITY_BACKLINK_METRICS_SOURCE=csv_legacy
   then redeploy (re-enables CSV/GitHub GET fallback and CSV POST).

Git: revert the commit that introduced DFS-default authority backlinks.
