From 67b259ca26fec612f19482419989378f70c8919a Mon Sep 17 00:00:00 2001
From: alanranger <info@alanranger.com>
Date: Tue, 23 Dec 2025 17:04:07 +0000
Subject: [PATCH] Fix commit 5866276: Comment out broken references to
 non-existent loadAuditHistoryAndRenderKpis function

---
 audit-dashboard.html | 34 +++++++---------------------------
 1 file changed, 7 insertions(+), 27 deletions(-)

diff --git a/audit-dashboard.html b/audit-dashboard.html
index af2b6d8..8cb62ab 100644
--- a/audit-dashboard.html
+++ b/audit-dashboard.html
@@ -15110,33 +15110,13 @@ <h3>🔍 Debug Log</h3>
               }
             }
             
-            const sparklineCanvas = document.getElementById('money-kpi-sparkline');
-            if (sparklineCanvas && typeof loadAuditHistoryAndRenderKpis === 'function') {
-              const rect = sparklineCanvas.getBoundingClientRect();
-              if (rect.width === 0 || rect.height === 0) {
-                debugLog('⚠ Money KPI sparkline: Canvas has zero dimensions, will retry', 'warn');
-                setTimeout(renderMoneyCharts, 200);
-                return;
-              }
-              
-              const propertyUrl = document.getElementById('propertyUrl')?.value;
-              if (propertyUrl) {
-                requestAnimationFrame(() => {
-                  requestAnimationFrame(() => {
-                    const canvas = document.getElementById('money-kpi-sparkline');
-                    if (canvas) {
-                      const rect = canvas.getBoundingClientRect();
-                      if (rect.width > 0 && rect.height > 0) {
-                        loadAuditHistoryAndRenderKpis(propertyUrl);
-                      } else {
-                        debugLog('⚠ Money KPI sparkline: Canvas still has zero dimensions, retrying...', 'warn');
-                        setTimeout(() => renderMoneyCharts(), 200);
-                      }
-                    }
-                  });
-                });
-              }
-            }
+            // KPI Tracker functionality not yet implemented at this commit (5866276)
+            // The loadAuditHistoryAndRenderKpis function was added in commit 48ae868
+            // Commenting out to prevent errors until that function is added
+            // const sparklineCanvas = document.getElementById('money-kpi-sparkline');
+            // if (sparklineCanvas && typeof loadAuditHistoryAndRenderKpis === 'function') {
+            //   ... (function doesn't exist yet)
+            // }
             };
             
             // Start rendering charts with retry logic
