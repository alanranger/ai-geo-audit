# Copy this file to .env and replace the encryption key (see README).
# Required: 32-byte hex (64 hex chars). Generate: openssl rand -hex 32
N8N_ENCRYPTION_KEY=

N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
TZ=Europe/London
