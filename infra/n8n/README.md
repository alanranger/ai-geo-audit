# n8n (Docker) — AI GEO Audit

- **`.env`** holds secrets and is git-ignored (create from `.env.example`).
- **UI** is bound to `127.0.0.1:5678` only; use an SSH tunnel to reach it safely.

Setup steps are given **one at a time** in chat (do not skip ahead).
