{
  "version": 1,
  "banding": "pinned_absolute_lost_clicks",
  "thresholds": {
    "HIGH": 15,
    "MEDIUM": 5
  },
  "allowedHttpStatusChips": [200, 301, 404],
  "notes": "lostClicks = impressions × max(0, expectedCtr(position) − actualCtr). Bands stable audit-to-audit unless page metrics cross fixed boundaries."
}
